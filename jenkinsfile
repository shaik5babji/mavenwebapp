pipeline {
  agent { label 'jenkins-agent-infrastructure' }
  environment {
    VER = VersionNumber([versionNumberString : '${BUILD_YEAR}.${BUILD_MONTH}.${BUILD_DAY}.${BUILDS_ALL_TIME}', projectStartDate : '2018-11-25']);
  }
}
